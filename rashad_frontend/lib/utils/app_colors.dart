import 'package:flutter/material.dart';

class AppColors {
  // Primary Dark Palette (Charcoal & Orange)
  static const Color primaryColor = Color(0xFFFF8A00); // Deep Orange
  static const Color primaryDark = Color(0xFFE67C00);
  static const Color primaryLight = Color(0xFFFFB347);

  // Accent Colors
  static const Color accentColor = Color(0xFFFFD600); // Vivid Yellow-Orange
  static const Color accentLight = Color(0xFFFEE440);

  // Deep Backgrounds
  static const Color background = Color(0xFF0A0A0B); // Near Black
  static const Color surface = Color(0xFF1C1C1E);    // Dark Grey Surface
  static const Color cardBackground = Color(0xFF2C2C2E);

  // Status Colors (Dark Mode Optimized)
  static const Color success = Color(0xFF32D74B);
  static const Color warning = Color(0xFFFF9F0A);
  static const Color error = Color(0xFFFF453A);
  static const Color info = Color(0xFF64D2FF);

  // Task Status Colors
  static const Color taskPending = Color(0xFF8E8E93);
  static const Color taskInProgress = Color(0xFFFF9F0A);
  static const Color taskCompleted = Color(0xFF32D74B);

  // Text Colors (Apple Style Dark)
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8E8E93);
  static const Color textHint = Color(0xFF48484A);

  // Category Colors (Vibrant)
  static const Color categoryWork = Color(0xFFFF375F); // Pinkish Red
  static const Color categoryPersonal = Color(0xFFBF5AF2); // Indigo/Purple
  static const Color categoryUrgent = Color(0xFFFF453A); // Red
  static const Color categoryFun = Color(0xFFFFD600); // Yellow

  // Role Colors
  static const Color adminRole = Color(0xFFFF375F);
  static const Color userRole = Color(0xFFFF9F0A);
}
